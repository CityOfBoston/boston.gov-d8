{#
 * @file
 * Returns the HTML for a Topic node displayed via Featured Guides view mode.
 *
 * Complete documentation for this file is available online.
 *
 * @see https%0%
 #}

{# TODO: review variable assignment:  guide_themes = array( #}
   'ob',
   'cb',
   'r',
   'cb',
   'cb',
   'r',
   'cb',
   'ob',
   'ob',
   'cb',
   'r',
   'cb',
   'cb',
   'r',
   'cb',
   'ob',
 )

 if (!$GLOBALS.featured_guide_count) {
   $GLOBALS.featured_guide_count = 0
 } else {
   $GLOBALS.featured_guide_count++
 }

{# TODO: review variable assignment:  guide_index = $GLOBALS.featured_guide_count + 1 #}
{# TODO: review variable assignment:  guide_theme = $guide_themes[$GLOBALS.featured_guide_count] #}

<a href="{{ url("node/{node >nid}") }}" class="cdfg cdfg--{{ guide_theme }}" style="background-image: url({{ content.field_thumbnail) }}">
  <div class="cdfg-c">
    <div class="cdfg-i"><span>{{ guide_index }}</span></div>
    <div class="cdfg-ic">
      <div class="cdfg-d">Guide:</div>
      <div class="cdfg-t">{{ title }}</div>
    </div>
  </div>
</a>
