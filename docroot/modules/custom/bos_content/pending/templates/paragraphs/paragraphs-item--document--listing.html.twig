{#
 * @file
 * External Link View Mode: Token  Field template.
 *
 * If the $field_title is not set, defaults to the filename.
 #}
?>

{% if content.field_title %}
{# TODO: review variable assignment:   {{ field_title = $content.field_title }} #}
{% else %}
{# TODO: review variable assignment:   {{ field_title = $document_filename }} #}
{% endif %}

<?php
{# TODO: review variable assignment: detail_item_variables = array( #}
  'label': 'Downloads:',
  'body': "<ul><li>" . "<a href='$document_link' title= '$field_title' > field_title ". "</a>" . "</li></ul>",
  'classes': array(
    'icon': 'icon-download',
    'body': 'detail-item__body--secondary',
  ),
)

theme('detail_item', $detail_item_variables)
