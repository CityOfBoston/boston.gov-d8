{#
 * @file
 * Default simple view template to display a list of rows.
 *
 * @ingroup views_templates
 #}
<div class="grid-wrapper clearfix">
{# TODO: review variable assignment:   {{ foreach (rows as id: $row):  }} #}
    {{ row }}
  {{ endforeach }}
</div>
