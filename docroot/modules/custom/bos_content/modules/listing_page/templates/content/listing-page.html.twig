{#
/**
 * @file
 * Default theme implementation to present a listing page entity.
 *
 * This template is used when viewing a registered listing page's page,
 * e.g., /admin/content/listing-page)/123. 123 being the listing page's ID.
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 *   print a subset such as 'content.title'.
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_listing_page()
 */
#}
<article{{ attributes }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</article>
