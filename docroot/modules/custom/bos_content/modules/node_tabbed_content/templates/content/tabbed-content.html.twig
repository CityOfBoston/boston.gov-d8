{#
/**
 * @file
 * Default theme implementation to present a tabbed content entity.
 *
 * This template is used when viewing a registered tabbed content's page,
 * e.g., /admin/content/tabbed-content)/123. 123 being the tabbed content's ID.
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 *   print a subset such as 'content.title'.
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_tabbed_content()
 */
#}
<article{{ attributes }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</article>
