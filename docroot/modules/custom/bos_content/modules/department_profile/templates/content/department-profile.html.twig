{#
/**
 * @file
 * Default theme implementation to present a department profile entity.
 *
 * This template is used when viewing a registered department profile's page,
 * e.g., /admin/content/department-profile)/123. 123 being the department profile's ID.
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 *   print a subset such as 'content.title'.
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_department_profile()
 */
#}
<article{{ attributes }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</article>
