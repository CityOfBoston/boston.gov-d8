{#
/**
 * @file
 * Default implementation for field_sidebar_components field render.
 */
#}

{% for item in items %}
  {# Hack to keep from wrapping the commission info in an extra
     "list-item", since it’s made up of several list items.
  #}
  {% set paragraph = item.content['#paragraph'] %}
  {% if paragraph %}
    <div class="si-list-item list-item">
      {{ item.content }}
    </div>
  {% else %}
    <div class="si-list-item list-item"{{ attributes }}>
      {{ item }}
    </div>
  {% endif %}

{% endfor %}
