import React from 'react';
import { render } from '@testing-library/react';
import Callout from './index';

describe( 'Callout', () => {
  it( 'Renders', () => {
    render( <Callout /> );
  } );
} );
